"""
Valid Parentheses
=================
Given a string `s` containing only the characters `(`, `)`, `{`, `}`,
`[`, `]`, determine whether the brackets are balanced. A string is
balanced when:
  - Every opening bracket is closed by the same type of bracket.
  - Brackets close in the correct (LIFO) order.
  - Every opening bracket has a matching closing bracket.

The empty string is balanced.

Example:
    Input:  s = "(){}"
    Output: True

    Input:  s = "(]"
    Output: False

    Input:  s = "(("
    Output: False
"""


def solve(s):
    pass
