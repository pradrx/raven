<!--
specs/<slug>.md

Edit the sections below, then run `/raven-new <slug>` to generate the
question + tests pair from this spec. Raven leaves this file alone
after generation; commit it as an audit trail or gitignore the
directory as you prefer.

Empty sections are fine — the agent falls back to its own judgment.
-->

# <slug>

## Problem
<One or two paragraphs. Inputs, outputs, what `solve` must return.
Define any non-obvious terms.>

## Constraints
<Input bounds, types guaranteed, what is explicitly out of scope.>

## Illustrative examples
<Inputs/outputs to put in the docstring. These will NOT be reused as
test cases — the agent writes distinct hidden cases.>

## Edge cases to cover
<Specific cases you want represented in the hidden tests
(empty input, duplicates, negatives, ordering quirks, max bounds, ...).>

## Difficulty hint
<easy | medium | hard | (leave blank for the agent to choose)>

## Topics hint
<comma-separated tags from AGENTS.md vocabulary; leave blank for the agent>

## Notes
<Anti-patterns to avoid, simpler solutions you want disallowed,
specific phrasing for the docstring, anything else.>
